version: "3"
services:
    nginx:
        restart: always
        build: 
            context: ./nginx
            dockerfile: Dockerfile
        ports:
            - '8082:80'

    api:
        build: 
            context: ./Server/
            dockerfile: Dockerfile
        image: api
        ports: 
            - 8081:8081
        networks: 
            - app-network

    mobile_client:
        build:
            context: ./Mobile/
            dockerfile: Dockerfile
        image: mobile
        depends_on: 
            - api
            - nginx

    webapp:
        build: 
            context: ./web_app/
            dockerfile: Dockerfile
        image: web_client
        ports: 
            - 8080:8080
        depends_on: 
            - mobile_client
            - api

networks:
    app-network:
        driver: bridge